{
  "name": "community-yield-pool-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@tanstack/react-query": "^5.90.7",
    "@web3modal/wagmi": "^4.1.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "5.0.1",
    "viem": "^2.38.6",
    "wagmi": "^2.19.2",
    "web-vitals": "^2.1.4"
  },
  "devDependencies": {
    "ajv": "8.12.0"
  },
  "scripts": {
    "start": "set GENERATE_SOURCEMAP=false && set WDS_SOCKET_PORT=0 && set FAST_REFRESH=false && set DISABLE_ESLINT_PLUGIN=true && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "install-deps": "npm install --legacy-peer-deps"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "overrides": {
    "ajv": "8.12.0"
  }
}

